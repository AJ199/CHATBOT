# CHATBOT

Minimal ChatGPT-like web app demonstrating token-level streaming from OpenAI -> FastAPI -> WebSocket -> React.

## Features
- Multi-turn chat (in-memory)
- Token-level streaming (WebSocket)
- Simple React UI with streaming display
- Docker + docker-compose for local dev

